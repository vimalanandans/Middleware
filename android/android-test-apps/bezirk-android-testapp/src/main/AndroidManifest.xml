<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.bezirk.middleware.android.testApp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bezirk_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="icon, label, theme">
        <receiver android:name="com.bezirk.middleware.android.ZirkMessageReceiver">
            <intent-filter>
                <action android:name="com.bezirk.middleware.broadcast"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </receiver>

        <activity android:name="com.bezirk.middleware.android.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>