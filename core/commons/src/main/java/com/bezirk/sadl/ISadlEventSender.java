package com.bezirk.sadl;

/*import EventLedger;*/

/**
 * Platform independent API's used by the SADL for sending Event messages 
 */
/*@Deprecated // this functionality is moved to Event Sender Thread. Remove this. Vimal
public interface ISadlEventSender {
	

	 //* Checks and sends the event on the wire or bridges the event
	 //* @param ecMessage the message that goes on the wire
	 //* @return true if the message has to go on wire, false otherwise.
	 //
    @Deprecated // this functionality is moved to Event Sender Thread. Remove this. Vimal
	public boolean sendEvent(EventLedger ecMessage);
}*/
