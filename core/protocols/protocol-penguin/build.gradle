apply plugin: 'java'

jar {
    manifest {
        attributes 'Implementation-Title': 'penguin-protocol', 'Implementation-Version': version
    }
}

dependencies {
    compile project(':core:bezirk-middleware-api')
    compile project(':core:protocols:protocol-dragonfly')
    testCompile 'junit:junit:4.12'
}
